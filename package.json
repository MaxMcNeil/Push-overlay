{
  "name": "journal-du-non-overlay",
  "version": "1.0.0",
  "description": "Pipeline automatisé : vidéo -> transcription locale -> accroches -> overlay OBS (horizontal/vertical) publié sur GitHub Pages.",
  "type": "module",
  "private": true,
  "scripts": {
    "transcribe": "node scripts/transcribe.mjs",
    "highlights": "node scripts/extract-highlights.mjs",
    "generate": "node scripts/generate-overlay.mjs",
    "build": "npm run transcribe && npm run highlights && npm run generate"
  },
  "dependencies": {
    "nodejs-whisper": "^0.1.18"
  }
}
